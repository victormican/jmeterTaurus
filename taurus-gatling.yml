execution:
  - executor: shellexec
    scenario: run-gatling

scenarios:
  run-gatling:
    script: |
      cd gatling
      mvn gatling:test -Dgatling.simulationClass=${SIMULATION}


reporting:
  - module: console
  - module: final-stats
