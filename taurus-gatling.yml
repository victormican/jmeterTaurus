settings:
  artifacts-dir: artifacts

execution:
  - executor: shellexec
    scenario: gatling-cli

scenarios:
  gatling-cli:
    script:
      - cd gatling
      - mvn gatling:test -Dgatling.simulationClass=${SIMULATION}

reporting:
  - module: console
  - module: final-stats
