execution:
  - executor: k6
    scenario: k6_test
    vus: 10
    duration: 1m

scenarios:
  k6_test:
    script: tests/load/test_k6.js

reporting:
  - module: final-stats

passfail:
  - criteria:
      - avg-rt < 500ms
      - fail > 0%
