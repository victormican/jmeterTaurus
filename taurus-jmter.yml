execution:
  - executor: jmeter
    scenario: test-jmx

scenarios:
  test-jmx:
    script: test.jmx

reporting:
  - module: console

settings:
  artifacts-dir: performance-results
  check-updates: false

modules:
  jmeter:
    path: /opt/jmeter
    version: 5.6.3
